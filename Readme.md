# Resona - The simple api callback scheduler

## Features

- custom endpoints and payloads
- register a callback for a specific time
- register a callback to be triggered after some delay
- register recurring callbacks (cron)
